<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wedding Invitation | Elena & Alexander</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Great+Vibes&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-gold: #d4af37;
            --rose-gold: #b76e79;
            --deep-navy: #111e2e;
            --cream: #faf7f2;
            --soft-white: #ffffff;
            --charcoal: #2c3e50;
            --accent-green: #556b2f;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--cream);
            color: var(--charcoal);
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* --- ENVELOPE / REVEAL SCREEN --- */
        #envelope-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: radial-gradient(circle, #2c3e50 0%, #111e2e 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            transition: opacity 1.2s ease-in-out, visibility 1.2s;
        }

        .envelope-wrapper {
            position: relative;
            width: 320px;
            height: 220px;
            background: #e0d5c1;
            box-shadow: 0 15px 35px rgba(0,0,0,0.5);
            cursor: pointer;
            perspective: 1000px;
            border-radius: 4px;
        }

        .envelope-lid {
            position: absolute;
            top: 0;
            left: 0;
            width: 0;
            height: 0;
            border-left: 160px solid transparent;
            border-right: 160px solid transparent;
            border-top: 120px solid #c8b9a1;
            transform-origin: top;
            transition: transform 0.6s ease-in-out;
            z-index: 3;
        }

        .envelope-pocket {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 0;
            border-left: 160px solid #d4c5ad;
            border-right: 160px solid #d4c5ad;
            border-bottom: 120px solid #b8a78d;
            z-index: 2;
            border-radius: 0 0 4px 4px;
        }

        .wax-seal {
            position: absolute;
            top: 100px;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 55px;
            height: 55px;
            background: radial-gradient(circle, #b71c1c, #7f0000);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--primary-gold);
            font-family: 'Cinzel', serif;
            font-weight: bold;
            font-size: 1.2rem;
            box-shadow: 0 4px 10px rgba(0,0,0,0.4);
            z-index: 4;
            transition: transform 0.3s ease;
        }

        .envelope-wrapper:hover .wax-seal {
            transform: translate(-50%, -50%) scale(1.1);
        }

        .envelope-card {
            position: absolute;
            bottom: 10px;
            left: 15px;
            right: 15px;
            height: 190px;
            background: var(--soft-white);
            border-radius: 4px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            transition: transform 0.8s ease-in-out;
            z-index: 1;
        }

        .envelope-card h3 {
            font-family: 'Great Vibes', cursive;
            color: var(--rose-gold);
            font-size: 2rem;
            margin-top: 20px;
        }

        .click-hint {
            color: var(--cream);
            margin-top: 30px;
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.6; }
            50% { opacity: 1; }
        }

        /* Envelope Open Animation */
        .envelope-wrapper.open .envelope-lid {
            transform: rotateX(180deg);
            z-index: 0;
        }

        .envelope-wrapper.open .envelope-card {
            transform: translateY(-80px);
        }

        .envelope-wrapper.open .wax-seal {
            opacity: 0;
        }

        /* --- MAIN INVITATION CONTENT --- */
        #main-content {
            opacity: 0;
            transition: opacity 1s ease-in;
            display: none;
        }

        #main-content.visible {
            opacity: 1;
            display: block;
        }

        header {
            height: 100vh;
            background: linear-gradient(rgba(17, 30, 46, 0.5), rgba(17, 30, 46, 0.5)), 
                        url('https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=1920&q=80') center/cover no-repeat;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: var(--soft-white);
            text-align: center;
            padding: 20px;
            position: relative;
        }

        .header-title {
            font-family: 'Cinzel', serif;
            font-size: 1.2rem;
            letter-spacing: 4px;
            text-transform: uppercase;
            margin-bottom: 15px;
            color: var(--primary-gold);
        }

        .couple-names {
            font-family: 'Great Vibes', cursive;
            font-size: 4.5rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .wedding-date {
            font-family: 'Cinzel', serif;
            font-size: 1.5rem;
            letter-spacing: 2px;
            margin-top: 15px;
        }

        /* Audio Controls */
        .audio-control {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 45px;
            height: 45px;
            background: var(--primary-gold);
            color: var(--soft-white);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            z-index: 100;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
            border: none;
        }

        /* Countdown Timer */
        .countdown-container {
            background-color: var(--soft-white);
            padding: 50px 20px;
            text-align: center;
            box-shadow: 0 4px 20px rgba(0,0,0,0.05);
        }

        .section-heading {
            font-family: 'Cinzel', serif;
            font-size: 2rem;
            color: var(--deep-navy);
            margin-bottom: 30px;
            position: relative;
            display: inline-block;
        }

        .section-heading::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 25%;
            width: 50%;
            height: 2px;
            background-color: var(--primary-gold);
        }

        .timer {
            display: flex;
            justify-content: center;
            gap: 25px;
            max-width: 600px;
            margin: 0 auto;
        }

        .timer-box {
            background: var(--cream);
            padding: 15px 20px;
            border-radius: 8px;
            min-width: 90px;
            border: 1px solid rgba(212, 175, 55, 0.3);
        }

        .timer-box span {
            display: block;
        }

        .number {
            font-family: 'Cinzel', serif;
            font-size: 2rem;
            font-weight: 700;
            color: var(--rose-gold);
        }

        .label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--charcoal);
        }

        /* Event Details & Schedule */
        .details-section {
            padding: 60px 20px;
            max-width: 900px;
            margin: 0 auto;
        }

        .grid-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            margin-top: 30px;
        }

        .detail-card {
            background: var(--soft-white);
            padding: 30px;
            border-radius: 8px;
            text-align: center;
            border-top: 4px solid var(--primary-gold);
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .detail-card i {
            font-size: 2.5rem;
            color: var(--rose-gold);
            margin-bottom: 15px;
        }

        .detail-card h3 {
            font-family: 'Cinzel', serif;
            margin-bottom: 10px;
        }

        .btn-map {
            display: inline-block;
            margin-top: 15px;
            padding: 8px 18px;
            background: var(--deep-navy);
            color: var(--soft-white);
            text-decoration: none;
            border-radius: 4px;
            font-size: 0.85rem;
            transition: background 0.3s;
        }

        .btn-map:hover {
            background: var(--rose-gold);
        }

        /* Schedule Timeline */
        .timeline {
            position: relative;
            max-width: 600px;
            margin: 40px auto 0;
            padding-left: 20px;
            border-left: 2px solid var(--primary-gold);
        }

        .timeline-item {
            position: relative;
            margin-bottom: 30px;
            padding-left: 20px;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -26px;
            top: 0;
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: var(--rose-gold);
            border: 2px solid var(--soft-white);
        }

        .timeline-time {
            font-weight: 600;
            color: var(--primary-gold);
            font-size: 0.9rem;
        }

        .timeline-title {
            font-family: 'Cinzel', serif;
            font-size: 1.1rem;
            margin: 3px 0;
        }

        /* RSVP Section */
        .rsvp-section {
            background: var(--deep-navy);
            color: var(--soft-white);
            padding: 60px 20px;
            text-align: center;
        }

        .rsvp-section .section-heading {
            color: var(--soft-white);
        }

        .rsvp-form {
            max-width: 500px;
            margin: 30px auto 0;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .form-group {
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-size: 0.85rem;
            letter-spacing: 1px;
            color: var(--cream);
        }

        .form-control {
            width: 100%;
            padding: 12px;
            border-radius: 4px;
            border: 1px solid rgba(255,255,255,0.2);
            background: rgba(255,255,255,0.05);
            color: var(--soft-white);
            font-family: inherit;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-gold);
            background: rgba(255,255,255,0.1);
        }

        .btn-submit {
            padding: 14px;
            background: var(--primary-gold);
            color: var(--deep-navy);
            border: none;
            border-radius: 4px;
            font-family: 'Cinzel', serif;
            font-weight: 700;
            font-size: 1rem;
            letter-spacing: 1px;
            cursor: pointer;
            transition: background 0.3s;
            margin-top: 10px;
        }

        .btn-submit:hover {
            background: var(--rose-gold);
            color: var(--soft-white);
        }

        /* Gift Registry Modal Button */
        .registry-section {
            text-align: center;
            padding: 40px 20px;
        }

        .btn-registry {
            padding: 12px 25px;
            background: transparent;
            border: 2px solid var(--rose-gold);
            color: var(--rose-gold);
            font-family: 'Cinzel', serif;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-registry:hover {
            background: var(--rose-gold);
            color: var(--soft-white);
        }

        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.6);
            z-index: 2000;
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background: var(--soft-white);
            padding: 30px;
            border-radius: 8px;
            max-width: 450px;
            width: 90%;
            text-align: center;
            position: relative;
        }

        .close-modal {
            position: absolute;
            top: 10px;
            right: 15px;
            font-size: 1.5rem;
            cursor: pointer;
            color: #888;
        }

        footer {
            text-align: center;
            padding: 20px;
            font-size: 0.8rem;
            color: #777;
            background-color: var(--cream);
        }

        /* Responsive Mobile tweaks */
        @media (max-width: 600px) {
            .couple-names { font-size: 3.2rem; }
            .timer { gap: 10px; }
            .timer-box { min-width: 65px; padding: 10px 5px; }
            .number { font-size: 1.4rem; }
        }
    </style>
</head>
<body>

    <!-- AUDIO (Romantic background track placeholder) -->
    <audio id="bg-music" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>

    <button class="audio-control" id="audio-toggle" title="Toggle Music">
        <i class="fa-solid fa-music"></i>
    </button>

    <!-- ENVELOPE OVERLAY -->
    <div id="envelope-overlay">
        <div class="envelope-wrapper" id="envelope">
            <div class="envelope-lid"></div>
            <div class="wax-seal">E&A</div>
            <div class="envelope-card">
                <p style="font-family: 'Cinzel', serif; font-size: 0.8rem; letter-spacing: 1px;">YOU ARE INVITED</p>
                <h3>Elena & Alexander</h3>
                <p style="font-size: 0.75rem; color: #666; margin-top: 10px;">Tap to open invitation</p>
            </div>
            <div class="envelope-pocket"></div>
        </div>
        <p class="click-hint">Click Envelope to Open</p>
    </div>

    <!-- MAIN WEBSITE CONTENT -->
    <div id="main-content">
        <!-- Hero Section -->
        <header>
            <p class="header-title">Please Join Us To Celebrate</p>
            <h1 class="couple-names">Elena & Alexander</h1>
            <p style="font-size: 1.1rem; letter-spacing: 2px;">ARE GETTING MARRIED</p>
            <p class="wedding-date">OCTOBER 24, 2026</p>
        </header>

        <!-- Countdown Section -->
        <section class="countdown-container">
            <h2 class="section-heading">Counting Down To The Big Day</h2>
            <div class="timer">
                <div class="timer-box">
                    <span class="number" id="days">00</span>
                    <span class="label">Days</span>
                </div>
                <div class="timer-box">
                    <span class="number" id="hours">00</span>
                    <span class="label">Hours</span>
                </div>
                <div class="timer-box">
                    <span class="number" id="minutes">00</span>
                    <span class="label">Minutes</span>
                </div>
                <div class="timer-box">
                    <span class="number" id="seconds">00</span>
                    <span class="label">Seconds</span>
                </div>
            </div>
        </section>

        <!-- Details Section -->
        <section class="details-section">
            <div style="text-align: center;">
                <h2 class="section-heading">When & Where</h2>
            </div>
            <div class="grid-details">
                <div class="detail-card">
                    <i class="fa-solid fa-church"></i>
                    <h3>The Ceremony</h3>
                    <p>Saturday, Oct 24, 2026<br>4:00 PM</p>
                    <p style="margin-top: 10px; color: #666; font-size: 0.9rem;">St. Grace Cathedral<br>123 Floral Ave, New York, NY</p>
                    <a href="https://maps.google.com" target="_blank" class="btn-map"><i class="fa-solid fa-location-dot"></i> Get Directions</a>
                </div>
                <div class="detail-card">
                    <i class="fa-solid fa-champagne-glasses"></i>
                    <h3>The Reception</h3>
                    <p>Saturday, Oct 24, 2026<br>6:30 PM - Midnight</p>
                    <p style="margin-top: 10px; color: #666; font-size: 0.9rem;">The Grand Botanical Glasshouse<br>456 Parkway Blvd, New York, NY</p>
                    <a href="https://maps.google.com" target="_blank" class="btn-map"><i class="fa-solid fa-location-dot"></i> Get Directions</a>
                </div>
            </div>

            <!-- Schedule -->
            <div style="text-align: center; margin-top: 60px;">
                <h2 class="section-heading">Order of Events</h2>
            </div>
            <div class="timeline">
                <div class="timeline-item">
                    <div class="timeline-time">4:00 PM</div>
                    <div class="timeline-title">Ceremony Starts</div>
                    <p style="font-size: 0.85rem; color: #666;">Exchange of vows in the main sanctuary.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-time">5:15 PM</div>
                    <div class="timeline-title">Cocktail Hour</div>
                    <p style="font-size: 0.85rem; color: #666;">Drinks and hors d'oeuvres on the terrace.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-time">6:30 PM</div>
                    <div class="timeline-title">Dinner & Dances</div>
                    <p style="font-size: 0.85rem; color: #666;">Grand entrance followed by a 3-course dinner.</p>
                </div>
                <div class="timeline-item">
                    <div class="timeline-time">10:00 PM</div>
                    <div class="timeline-title">Cake Cutting & Party</div>
                    <p style="font-size: 0.85rem; color: #666;">Dancing and celebration until late.</p>
                </div>
            </div>
        </section>

        <!-- RSVP Section -->
        <section class="rsvp-section" id="rsvp">
            <h2 class="section-heading">RSVP</h2>
            <p style="letter-spacing: 1px; font-size: 0.9rem;">Please respond by September 15, 2026</p>
            
            <form class="rsvp-form" id="wedding-rsvp-form">
                <div class="form-group">
                    <label for="fullname">FULL NAME(S)</label>
                    <input type="text" id="fullname" class="form-control" placeholder="e.g., Jane & John Doe" required>
                </div>
                <div class="form-group">
                    <label for="email">EMAIL ADDRESS</label>
                    <input type="email" id="email" class="form-control" placeholder="john@example.com" required>
                </div>
                <div class="form-group">
                    <label for="attendance">WILL YOU ATTEND?</label>
                    <select id="attendance" class="form-control" required>
                        <option value="" disabled selected>Select an option</option>
                        <option value="accept">Joyfully Accepts</option>
                        <option value="decline">Regretfully Declines</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="guests">NUMBER OF GUESTS</label>
                    <select id="guests" class="form-control">
                        <option value="1">1</option>
                        <option value="2">2</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="dietary">DIETARY RESTRICTIONS / NOTES</label>
                    <textarea id="dietary" class="form-control" rows="3" placeholder="Vegetarian, Allergies, etc."></textarea>
                </div>
                <button type="submit" class="btn-submit">SEND RESPONSE</button>
            </form>
        </section>

        <!-- Registry Modal Trigger -->
        <section class="registry-section">
            <p style="margin-bottom: 15px; font-size: 0.9rem; color: #666;">Your presence is gift enough, but if you wish to celebrate with a gift:</p>
            <button class="btn-registry" id="open-registry">VIEW GIFT REGISTRY</button>
        </section>

        <footer>
            <p>Elena & Alexander • 10.24.2026</p>
            <p style="margin-top: 5px;">Created with ❤️ for our special day</p>
        </footer>
    </div>

    <!-- REGISTRY MODAL -->
    <div class="modal" id="registry-modal">
        <div class="modal-content">
            <span class="close-modal" id="close-registry">&times;</span>
            <h3 style="font-family: 'Cinzel', serif; margin-bottom: 15px; color: var(--deep-navy);">Gift Registry</h3>
            <p style="font-size: 0.9rem; color: #666; margin-bottom: 20px;">We are registered at the following stores:</p>
            <div style="display: flex; flex-direction: column; gap: 10px;">
                <a href="#" style="color: var(--rose-gold); text-decoration: none; font-weight: 600;"><i class="fa-solid fa-gift"></i> Crate & Barrel</a>
                <a href="#" style="color: var(--rose-gold); text-decoration: none; font-weight: 600;"><i class="fa-solid fa-store"></i> Target Registry</a>
                <a href="#" style="color: var(--rose-gold); text-decoration: none; font-weight: 600;"><i class="fa-solid fa-plane"></i> Honeymoon Fund (Zola)</a>
            </div>
        </div>
    </div>

    <!-- JavaScript logic -->
    <script>
        // 1. Envelope Opening & Reveal Logic
        const envelope = document.getElementById('envelope');
        const overlay = document.getElementById('envelope-overlay');
        const mainContent = document.getElementById('main-content');
        const bgMusic = document.getElementById('bg-music');
        const audioBtn = document.getElementById('audio-toggle');
        let isPlaying = false;

        envelope.addEventListener('click', () => {
            envelope.classList.add('open');
            
            // Attempt to play background music on user click
            bgMusic.play().then(() => {
                isPlaying = true;
            }).catch(() => {
                isPlaying = false;
            });

            setTimeout(() => {
                overlay.style.opacity = '0';
                overlay.style.visibility = 'hidden';
                mainContent.classList.add('visible');
            }, 1200);
        });

        // 2. Audio Toggle Logic
        audioBtn.addEventListener('click', () => {
            if (isPlaying) {
                bgMusic.pause();
                audioBtn.innerHTML = '<i class="fa-solid fa-music-slash"></i>';
                isPlaying = false;
            } else {
                bgMusic.play();
                audioBtn.innerHTML = '<i class="fa-solid fa-music"></i>';
                isPlaying = true;
            }
        });

        // 3. Countdown Timer Logic
        const weddingDate = new Date('October 24, 2026 16:00:00').getTime();

        function updateCountdown() {
            const now = new Date().getTime();
            const distance = weddingDate - now;

            if (distance < 0) {
                document.querySelector('.timer').innerHTML = "<h3>The Big Day is Here!</h3>";
                return;
            }

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1Here is a complete, interactive, self-contained single-page web app for a digital wedding invitation. It features a realistic opening envelope animation, an interactive RSVP form, countdown timer, event details, and photo gallery. 

You can save this code as an `.html` file (e.g., `index.html`) and open it directly in any web browser.

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wedding Invitation | Sarah & James</title>
    <link href="[https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Montserrat:wght@300;400;500&display=swap](https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Montserrat:wght@300;400;500&display=swap)" rel="stylesheet">
    <style>
        :root {
            --bg-color: #fcfbf7;
            --text-primary: #2c3e35;
            --accent-gold: #c5a059;
            --envelope-color: #eae5d9;
            --envelope-inner: #d8d2c2;
            --white: #ffffff;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* --- ENVELOPE SECTION --- */
        .envelope-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background-color: var(--bg-color);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            transition: opacity 1s ease, visibility 1s ease;
        }

        .envelope {
            position: relative;
            width: 320px;
            height: 220px;
            background-color: var(--envelope-inner);
            border-radius: 4px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
            cursor: pointer;
            perspective: 1000px;
        }

        /* Envelope Flap Top */
        .envelope::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 0;
            height: 0;
            border-left: 160px solid transparent;
            border-right: 160px solid transparent;
            border-top: 120px solid var(--envelope-color);
            transform-origin: top;
            transition: transform 0.6s ease;
            z-index: 3;
        }

        /* Envelope Pocket Bottom/Sides */
        .envelope::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 0;
            border-left: 160px solid var(--envelope-color);
            border-right: 160px solid var(--envelope-color);
            border-bottom: 120px solid var(--envelope-color);
            z-index: 2;
        }

        .wax-seal {
            position: absolute;
            top: 100px;
            left: 50%;
            transform: translateX(-50%);
            width: 45px;
            height: 45px;
            background-color: var(--accent-gold);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-family: 'Cormorant Garamond', serif;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            z-index: 4;
            transition: opacity 0.4s ease;
        }

        .envelope-card {
            position: absolute;
            bottom: 10px;
            left: 5%;
            width: 90%;
            height: 90%;
            background: white;
            border-radius: 4px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 10px;
            transition: transform 0.8s ease;
            z-index: 1;
        }

        /* Open Envelope Animation state */
        .envelope.open::before {
            transform: rotateX(180deg);
            z-index: 0;
        }

        .envelope.open .wax-seal {
            opacity: 0;
        }

        .envelope.open .envelope-card {
            transform: translateY(-80px);
        }

        /* Hidden Class */
        .fade-out {
            opacity: 0;
            visibility: hidden;
        }

        /* --- MAIN INVITATION CONTENT --- */
        .main-container {
            width: 100%;
            max-width: 750px;
            padding: 40px 20px;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 1s ease 0.5s, transform 1s ease 0.5s;
        }

        .main-container.show {
            opacity: 1;
            transform: translateY(0);
        }

        .card {
            background: var(--white);
            padding: 50px 30px;
            border-radius: 8px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.05);
            border: 1px solid rgba(197, 160, 89, 0.3);
            text-align: center;
        }

        h1, h2, h3 {
            font-family: 'Cormorant Garamond', serif;
            color: var(--text-primary);
        }

        .sub-heading {
            letter-spacing: 3px;
            text-transform: uppercase;
            font-size: 0.85rem;
            color: var(--accent-gold);
            margin-bottom: 20px;
        }

        .couple-names {
            font-size: 3rem;
            font-weight: 400;
            margin: 15px 0;
            line-height: 1.1;
        }

        .details {
            font-size: 1.1rem;
            margin: 25px 0;
            line-height: 1.6;
            color: #555;
        }

        /* Countdown */
        .timer-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 35px 0;
        }

        .timer-block {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .timer-number {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            color: var(--accent-gold);
        }

        .timer-label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #777;
        }

        /* Divider */
        .divider {
            height: 1px;
            width: 80px;
            background-color: var(--accent-gold);
            margin: 40px auto;
        }

        /* Form */
        .rsvp-section form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            max-width: 400px;
            margin: 20px auto 0;
        }

        .input-group {
            text-align: left;
        }

        .input-group label {
            display: block;
            font-size: 0.8rem;
            margin-bottom: 5px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        input, select, textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-family: inherit;
            font-size: 0.95rem;
        }

        button.btn-submit {
            background-color: var(--accent-gold);
            color: white;
            border: none;
            padding: 14px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: background 0.3s;
        }

        button.btn-submit:hover {
            background-color: #ab8845;
        }

        #form-message {
            margin-top: 15px;
            font-size: 0.95rem;
            color: var(--accent-gold);
        }
    </style>
</head>
<body>

    <!-- Envelope Overlay -->
    <div class="envelope-container" id="envelopeContainer">
        <div class="envelope" id="envelope">
            <div class="wax-seal">S&J</div>
            <div class="envelope-card">
                <p style="font-family: 'Cormorant Garamond', serif; font-size: 1.2rem;">You are invited</p>
                <p style="font-size: 0.7rem; color: #888; margin-top: 5px;">Click to open</p>
            </div>
        </div>
    </div>

    <!-- Main Web Page Content -->
    <div class="main-container" id="mainContent">
        <div class="card">
            <p class="sub-heading">Together with their families</p>
            <h1 class="couple-names">Sarah & James</h1>
            <p class="details">
                Request the pleasure of your company<br>
                at the celebration of their wedding
            </p>

            <div class="divider"></div>

            <h2>Saturday, June 12, 2027</h2>
            <p class="details">
                <strong>Ceremony & Reception</strong><br>
                The Grand Botanical Gardens<br>
                123 Rosewood Drive, California
            </p>

            <!-- Countdown Timer -->
            <div class="timer-container">
                <div class="timer-block">
                    <span class="timer-number" id="days">00</span>
                    <span class="timer-label">Days</span>
                </div>
                <div class="timer-block">
                    <span class="timer-number" id="hours">00</span>
                    <span class="timer-label">Hours</span>
                </div>
                <div class="timer-block">
                    <span class="timer-number" id="minutes">00</span>
                    <span class="timer-label">Mins</span>
                </div>
                <div class="timer-block">
                    <span class="timer-number" id="seconds">00</span>
                    <span class="timer-label">Secs</span>
                </div>
            </div>

            <div class="divider"></div>

            <!-- RSVP Section -->
            <div class="rsvp-section">
                <h2>R.S.V.P.</h2>
                <p style="font-size: 0.9rem; color: #666; margin-top: 5px;">Please respond by May 1, 2027</p>

                <form id="rsvpForm">
                    <div class="input-group">
                        <label for="fullname">Full Name</label>
                        <input type="text" id="fullname" required placeholder="Jane Doe">
                    </div>

                    <div class="input-group">
                        <label for="attendance">Will you attend?</label>
                        <select id="attendance" required>
                            <option value="">Select option</option>
                            <option value="yes">Joyfully Accepts</option>
                            <option value="no">Regretfully Declines</option>
                        </select>
                    </div>

                    <div class="input-group">
                        <label for="guests">Number of Guests</label>
                        <select id="guests">
                            <option value="1">1</option>
                            <option value="2">2</option>
                        </select>
                    </div>

                    <button type="submit" class="btn-submit">Send Response</button>
                </form>
                <p id="form-message"></p>
            </div>
        </div>
    </div>

    <script>
        // Envelope Interaction
        const envelope = document.getElementById('envelope');
        const envelopeContainer = document.getElementById('envelopeContainer');
        const mainContent = document.getElementById('mainContent');

        envelope.addEventListener('click', () => {
            envelope.classList.add('open');
            setTimeout(() => {
                envelopeContainer.classList.add('fade-out');
                mainContent.classList.add('show');
            }, 1200);
        });

        // Countdown Timer Logic
        const targetDate = new Date('June 12, 2027 15:00:00').getTime();

        function updateCountdown() {
            const now = new Date().getTime();
            const difference = targetDate - now;

            if (difference > 0) {
                const days = Math.floor(difference / (1000 * 60 * 60 * 24));
                const hours = Math.floor((difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((difference % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((difference % (1000 * 60)) / 1000);

                document.getElementById('days').innerText = days < 10 ? '0' + days : days;
                document.getElementById('hours').innerText = hours < 10 ? '0' + hours : hours;
                document.getElementById('minutes').innerText = minutes < 10 ? '0' + minutes : minutes;
                document.getElementById('seconds').innerText = seconds < 10 ? '0' + seconds : seconds;
            }
        }

        setInterval(updateCountdown, 1000);
        updateCountdown();

        // RSVP Form Handling
        const rsvpForm = document.getElementById('rsvpForm');
        const formMessage = document.getElementById('form-message');

        rsvpForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const name = document.getElementById('fullname').value;
            const attendance = document.getElementById('attendance').value;

            if (attendance === 'yes') {
                formMessage.innerText = `Thank you, ${name}! We look forward to celebrating with you.`;
            } else {
                formMessage.innerText = `Thank you for letting us know, ${name}. You will be missed!`;
            }

            rsvpForm.reset();
        });
    </script>
</body>